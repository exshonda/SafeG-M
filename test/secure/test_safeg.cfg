/*
 *		SafeG-M 遷移テストのシステムコンフィギュレーションファイル
 */
#ifndef TOPPERS_OMIT_TECS
INCLUDE("tecsgen.cfg");
#else /* TOPPERS_OMIT_TECS */
INCLUDE("syssvc/syslog.cfg");
INCLUDE("syssvc/banner.cfg");
INCLUDE("syssvc/serial.cfg");
INCLUDE("syssvc/logtask.cfg");
#endif /* TOPPERS_OMIT_TECS */

#include "test_safeg.h"

CRE_TSK(MAIN_TASK,    { TA_ACT,  0, main_task,    MAIN_PRIORITY,    STK_SIZE, NULL });
CRE_TSK(RESTART_TASK, { TA_ACT,  0, restart_task, RESTART_PRIORITY, STK_SIZE, NULL });
CRE_TSK(HI_TASK,      { TA_NULL, 0, hi_task,      HI_PRIORITY,      STK_SIZE, NULL });
CRE_TSK(EXC_TASK,     { TA_NULL, 0, exc_task,     EXC_PRIORITY,     STK_SIZE, NULL });

CRE_CYC(TICK_CYC, { TA_STA, { TNFY_HANDLER, 0, tick_handler }, 100000, 0 });

#ifdef CPUEXC1
DEF_EXC(CPUEXC1, { TA_NULL, cpuexc_handler });
#endif /* CPUEXC1 */

/*
 *  D1: NS で発生した CPU 例外は HardFault(exc3) として Secure に入るため，
 *  exc3 にハンドラを登録する（未登録だと default_exc_handler の
 *  "Unregistered Exception" で停止する）．
 */
DEF_EXC(HARDFAULT_EXCNO, { TA_NULL, hardfault_handler });
